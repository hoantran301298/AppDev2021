#pragma once
#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS
#define MIN 3
#define MAX 20
#include <math.h>

int get_int(int, int);
long long int comb(long int, long int);
long long int fact(long int);
void pascal_triangle(int);
